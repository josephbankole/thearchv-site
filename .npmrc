; sharp 0.35 requires Node >=20.9.0 and npm only WARNS on an engines mismatch by default —
; the install then "succeeds" without sharp's prebuilt binaries and the build dies later at
; `import sharp` with a trace that points at nothing (2026-08-12 review). Make it fail at npm ci.
engine-strict=true
